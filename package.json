{
  "name": "epogen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass --include-path scss scss/index.scss css/styles.css",
    "uglify":"uglifycss css/styles.css > css/styles.min.css",
    "watch": "nodemon -e scss -x 'npm run css && npm run uglify'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "uglifycss": "^0.0.29"
  }
}
